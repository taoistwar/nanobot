I reached the maximum number of tool call iterations ({{ max_iterations }}) without completing the task. You can try breaking the task into smaller steps.
我已达到工具调用迭代次数上限（{{ max_iterations }}），但未完成任务。你可以尝试将任务拆分为更小的步骤。
